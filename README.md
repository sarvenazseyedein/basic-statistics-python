This project implements simple statistical functions in Python.

The program includes functions to calculate:

- Mean
- Median
- Mode
- Variance
- Standard deviation

The results are also saved in Excel files using the xlwt library.

Each function takes numbers as input and exports the results to an Excel file
